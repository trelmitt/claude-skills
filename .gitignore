.DS_Store
__pycache__/
*.pyc
# Local-only, never synced: confidential business/legal context that skills load at runtime
# (e.g. shadow-board-advisor/references/user-context.local.md — CIAA / acquisition / fundraising).
# The skills themselves now sync; only their *.local.md context files are held back.
*.local.md
